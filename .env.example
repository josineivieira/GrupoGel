# ============================================
# Backend Configuration
# ============================================

# Database (MongoDB)
MONGODB_URI=mongodb://localhost:27017/delivery-docs
# Para MongoDB Atlas (cloud) - RECOMENDADO para produção:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/delivery-docs

# JWT Secret - MUDE EM PRODUÇÃO!
# Gere uma chave forte: openssl rand -hex 32
JWT_SECRET=dev-secret-key-change-in-production-min-32-chars-here

# Server Configuration
PORT=5000
NODE_ENV=development
# Valores: development, staging, production

# File Uploads (opcional)
# Se vazio, usa /app/backend/uploads
# Para Render: use /mnt/data/uploads (persistent volume)
BACKEND_UPLOADS_DIR=/mnt/data/uploads

# Google Drive Integration (opcional)
# GOOGLE_CLIENT_ID=seu-client-id
# GOOGLE_CLIENT_SECRET=seu-client-secret

# AWS S3 (opcional)
# AWS_ACCESS_KEY_ID=seu-key
# AWS_SECRET_ACCESS_KEY=seu-secret
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=seu-bucket

# Email Configuration (opcional)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=seu-email@gmail.com
# SMTP_PASS=sua-senha-app
# SMTP_FROM=noreply@sua-empresa.com

# ============================================
# Frontend Configuration
# ============================================

# API Backend URL
REACT_APP_API_URL=http://localhost:5000/api
# Em produção: https://seu-dominio.com/api

# Frontend Environment
CI=false

# ============================================
# Render-Specific (se deployando em Render)
# ============================================
# Estos valores sao definidos automaticamente
# NODE_ENV=production
# PORT=10000 (ou qualquer porta Render atribuir)

# ============================================
# Notas Importantes
# ============================================
# 1. Criar arquivo .env na RAIZ do projeto (não dentro de backend/frontend)
# 2. Não fazer COMMIT deste arquivo (.env) - está no .gitignore
# 3. Criar .env separado para cada ambiente (dev, staging, prod)
# 4. Em produção, NUNCA use valores padrão
# 5. Guardar .env.production em local seguro (fora do git)
# 6. Usar preferentemente secrets.json ou variáveis do deployment

# ============================================
# Como Usar
# ============================================
# Local:
# 1. cp .env.example .env
# 2. Editar .env com seus valores
# 3. npm run dev
#
# Render:
# 1. Ir em Environment do serviço
# 2. Adicionar cada variável com seu valor
# 3. Fazer deploy
# 3. Para produção, usar variáveis de ambiente seguras
# 4. JWT_SECRET deve ser único e complexo para cada ambiente
